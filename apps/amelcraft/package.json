{
  "name": "amelcraft",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "npm run generate:code",
    "dev": "vite",
    "prebuild": "npm run generate:code",
    "build": "vite build",
    "prepreview": "npm run build",
    "preview": "vite preview",
    "generate:code": "node ./scripts/gen-assets.js",
    "test": "vitest"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.0.0",
    "vitest": "^3.2.4"
  }
}
