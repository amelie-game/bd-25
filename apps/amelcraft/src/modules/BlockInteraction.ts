import Phaser from "phaser";

export class BlockInteraction {
  constructor(/* TODO: dependencies */) {}
  tryPlaceBlock(tx: number, ty: number) {/* TODO */}
  startCollectBlock(tx: number, ty: number) {/* TODO */}
  cancelCollectBlock() {/* TODO */}
}